/**
 * FICHIER OBSOLÈTE — à supprimer avec le reste de src/utils/ (code mort,
 * aucun import hors de ce dossier). Son ancien contenu (JSX dans un .ts)
 * cassait `npm run typecheck` ; ce stub le neutralise en attendant la
 * suppression du dossier.
 */
export {};
