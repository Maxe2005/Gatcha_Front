/**
 * FICHIER OBSOLÈTE — à supprimer avec le reste de src/utils/ (code mort,
 * aucun import hors de ce dossier). L'ancien barrel importait des modules
 * inexistants (PortalExamples, PortalDebug) et cassait `npm run typecheck`.
 */
export {};
